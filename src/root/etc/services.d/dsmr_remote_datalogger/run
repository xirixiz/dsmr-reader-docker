#!/usr/bin/with-contenv sh

if [[ "${DATALOGGER_MODE}" = sender ]]; then
    echo "Starting DSMR Reader - remote datalogger..."
    cd /dsmr
    exec /usr/local/bin/python3 -u /dsmr/dsmr_datalogger_api_client.py
fi
